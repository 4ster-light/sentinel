name: Release Container

on:
  release:
    types: [published]

env:
  REGISTRY: ghcr.io
  IMAGE_NAME: ${{ github.repository }} # → ghcr.io/4ster-light/sentinel

jobs:
  build-and-push:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install Nix
        uses: DeterminateSystems/nix-installer-action@v14

      - name: Set up Nix cache
        uses: DeterminateSystems/magic-nix-cache-action@v8

      - name: Build container image
        run: |
          nix build .#container --out-link ./container-image

      - name: Log in to GHCR
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Load and push image
        run: |
          IMAGE="${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}"

          docker load < ./container-image

          # Retag from the 'latest' tag Nix baked in
          docker tag sentinel:latest "${IMAGE}:${{ github.event.release.tag_name }}"
          docker tag sentinel:latest "${IMAGE}:latest"

          docker push "${IMAGE}:${{ github.event.release.tag_name }}"
          docker push "${IMAGE}:latest"
